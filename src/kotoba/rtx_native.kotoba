(ns kotoba.rtx-native
  (:export [main adr-value phase-value kami-name-value status-value
            triggered-by-value nv-compat-primary nv-compat-secondary activated?]))

(defn main [] :i64 0)

(defn adr-value [] :string
  "ADR-2605261800")

(defn phase-value [] :string
  "R1.0-path-reservation")

(defn kami-name-value [] :string
  "kami-rtx-native")

(defn status-value [] :string
  "contingent-fallback-pending-viability-gate")

(defn triggered-by-value [] :string
  "Mitsuba 3 wgpu backend gate fail (R1.2)")

(defn nv-compat-primary [] :string
  "OptiX")

(defn nv-compat-secondary [] :string
  "RTX Renderer")

(defn activated? [] :bool
  false)
